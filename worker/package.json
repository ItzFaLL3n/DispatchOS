{
  "name": "dispatch-os-assistant-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Small always-on worker running the Claude Agent SDK for Dispatch OS's CRM assistant (spec 0003). Not part of the web/ Next.js app or its dependency tree — deploys separately, holds no Supabase credentials.",
  "scripts": {
    "start": "node index.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.260",
    "@anthropic-ai/sdk": "^0.123.0",
    "zod": "^4.0.0"
  }
}
